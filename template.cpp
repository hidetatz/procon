#include <bits/stdc++.h>

using namespace std;

// using ll = long long;

int main() {
  // int n;
  // cin >> n;
  //
  // vector<int> as(n);
  // for (int i = 0; i < n; i++) cin >> as[i];
  //
  // for (int i = 0; i < n; i++) cout << as[i] << " ";
  // cout << endl;
  //
  // cout << ans << endl;
}
